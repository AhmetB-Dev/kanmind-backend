"""Django application configuration for authentication."""

from django.apps import AppConfig


class AuthAppConfig(AppConfig):
    """Configure the authentication application."""

    name = "auth_app"
