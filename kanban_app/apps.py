"""Django application configuration for KanMind boards and tasks."""

from django.apps import AppConfig


class KanbanAppConfig(AppConfig):
    """Configure the kanban application."""

    name = "kanban_app"
